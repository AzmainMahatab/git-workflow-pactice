name: 'Setup Environment'
description: 'Checkout code and setup Python'

runs:
  using: "composite"  # <--- This is the magic keyword
  steps:
    - uses: actions/checkout@v7

    - uses: actions/setup-python@v7
      with:
        python-version: '3.14'
